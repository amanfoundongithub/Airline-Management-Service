from core.settings.service import ServiceSettings
from core.settings.runtime import RuntimeSettings
from core.settings.external import OpenFlightsSettings

service_settings = ServiceSettings()
runtime_settings = RuntimeSettings()
openflights_settings = OpenFlightsSettings()
